# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-dotenv>=1.0

# Claude / Anthropic
anthropic>=0.69

# Vector store (bundles a local ONNX MiniLM embedding model — no API key needed)
chromadb>=0.5

# Ingestion
pypdf>=4.2
youtube-transcript-api==0.6.2
httpx>=0.27
beautifulsoup4>=4.12

# TLS trust via the OS store (fixes cert issues behind proxies / in some containers)
truststore>=0.9
